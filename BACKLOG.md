# BACKLOG

Задачи, отложенные сознательно. Каждая запись понятна без переписки,
в которой возникла: что не так, где смотреть, почему решили не делать сразу.

Сделанное отсюда убирается, а не отмечается галочкой — история в git.

---

## Переименовать каталог `vk-style`

Корпус лежит в `40_ab_factory/vk-style/`, то есть имя реальной компании
стоит в пути каждого файла публичного репозитория. Для стороннего читателя
это не «в стиле VK» и не жанр — это имя работодателя автора.

Сегменты уже вычищены: пара `news` / `dzen` заменена на
`editorial` / `algorithmic` и в корпусе, и в генераторе. Каталог — последний
оставшийся след.

**Почему не сделали сразу.** Путь зашит в 17 местах двух репозиториев:

- в фабрике 11 — `README.md`, `tools/build_corpus_index.py`,
  `tools/build_replays_index.py`, `tools/export_trainer_cases.py`,
  `41_agents/ab_factory/README.md`, `42_workflows/ab_factory/README.md`;
- в `ab-decisions-bench` 6 — `compare_models.py`, `calib_analysis.py`,
  `compare_calib.py`, `tools/select_calib330.py`, `tools/select_calib80.py`,
  `README.md` с командой создания симлинка.

Плюс сам симлинк `data/corpus` в бенче указывает внутрь этого каталога.
Переименование посреди работы над бенчем порвало бы его прогоны, поэтому
отложено до публикации бенча — тогда правится один раз в обоих репозиториях.

**Порядок, когда дойдут руки:** переименовать каталог → заменить путь
в 17 местах → пересоздать симлинк → прогнать `selfcheck` на обоих корпусах
(должно остаться 100.0% и 42.7%) → пересобрать `docs/data/` индексы.

**Проверка, что ничего не забыто:** `grep -rn "vk-style"` по обоим
репозиториям возвращает пусто.

---

## Перевести `docs/pipeline.html` на два языка

Единственная страница сайта без переключателя языков. Написана по-русски
намертво: ноль атрибутов `data-t`. Остальные три (`index`, `corpus`,
`policy`) переведены полностью — 61, 58 и 90 строк словаря соответственно.

**Почему это не полчаса, как кажется.** В разметке всего 15 видимых
строк — заголовок, пять кнопок сценариев, подписи полей, шапки блоков.
Но `pipeline.html` не статическая страница, а калькулятор: весь текст,
который читатель видит **после** нажатия «Запустить конвейер», рождается
в JavaScript. Там ещё **~53 фразы**, вшитые в шаблонные литералы
вперемешку с вычислениями:

```js
'Итог', detail:'Все проверки пройдены — значимый uplift выше MDE'
'Итог', detail:'Недостаточно свидетельств', badge:'INVESTIGATE'
`CTR: ${ctr_dp.toFixed(1)}% ≤ порога ${POL.ctr_guardrail_pct}`
'Значимо (α=0.05)'   'НАРУШЕН'   'Размеры выборок > 0'
```

Механизм `data-t` их не достаёт: он подставляет перевод по атрибуту в
разметке, а эти строки появляются в рантайме. Нужна функция `t('key')`
и вынос всех фраз в словарь — то есть правка примерно пятидесяти мест
в скрипте, а не расстановка атрибутов.

**Оценка: 4–6 часов.** Отдельно проверить вёрстку: часть фраз стоит
внутри таблиц, где длина строки влияет на ширину колонок, и английский
там может оказаться и длиннее, и короче русского.

**Делать вместе с двумя другими задачами по сайту** — показом эталона
рядом с вердиктом и переводом сайта на корпус из 660 кейсов. Все три
трогают вывод результата, порознь придётся переписывать одно и то же
дважды.

Пока на главной, в третьем шаге секции «Try it in 30 seconds», стоит
честная пометка — только в английской версии, русскому читателю она
не нужна.

---

## Свести терминологию вердикта к `decision`

В проекте три слова про одно и то же — исход `ship` / `do_not_ship` /
`investigate`:

| Термин | README | index | corpus | policy |
|---|---|---|---|---|
| `verdict` | 0 | 8 | 15 | **29** |
| `decision` | 1 | **12** | **68** | 15 |
| «вердикт» | **6** | 2 | 3 | 0 |

Разнобой не косметический: на странице, которая называется Decision
Engine, преобладает `verdict`, а в Corpus Explorer — `decision`.

**Куда сводим — к `decision`.** Интерфейс должен следовать за данными,
а не заводить свой слой поверх них. В данных и коде канон уже такой:
поля `decision` и `expected_decision` в корпусе, артефакт `decision.json`,
агент `decision_agent.py`, счётчики `decision_counts`. Плюс так называются
и страница, и кнопка «Explain decision».

Русское «вердикт» остаётся: в русском оно точнее и с именами полей
не конфликтует.

**Не трогать:** схему ответа модели в `ab-decisions-bench` — там своя
`verdict`, и на ней стоят опубликованные статьи. Бейджи `DO NOT SHIP`,
`INVESTIGATE` не переводятся и не переименовываются: это термины,
как `p-value`.

**Объём:** 29 мест в `policy.html` плюс мелочи на index и corpus,
час-полтора. Отдельной задачей, чтобы не смешивать с переводом
`pipeline.html`: там `Pipeline verdict` уйдёт вместе с остальными.

**Проверка:** `grep -c verdict docs/*.html` даёт ноль везде, кроме
допустимых мест; сайт открывается, тексты согласованы в обоих языках.

---

## ПРИОРИТЕТНО: внутренняя несогласованность данных в 177 кейсах

Корпус опубликован под CC BY 4.0, на него можно ссылаться. Чинить нужно
сейчас, пока ссылок ноль.

### Что не сходится

Две независимые проверки, обе на корпусе `cases_mvp_v2`:

**1. Сегменты не складываются в итог — 81 кейс.** В строке `segment=all`
выручка не равна сумме строк по сегментам. Пример `case_041`: в контроле
сходится точно, в тесте итог 1 516 240 против суммы сегментов 1 428 105 —
расхождение 88 135 (5.8%) и 2345 неучтённых пользователей.

**2. Заявленный эффект не равен фактическому — 100 кейсов.** Поле
`revenue_effect_relative` расходится с изменением выручки на пользователя,
посчитанным из сырых колонок `revenue` и `n_users`. Худший случай —
расхождение знака: `case_032` заявляет +2.75%, по сырым данным −1.2%.

**Объединение: 177 кейсов из 660, то есть 27%.**

Поголовно затронуты пять механизмов: `simpson_paradox` 26/26,
`harking` 18/18, `heterogeneity` 18/18, `srm` 18/18, `underpowered` 16/16.

### `simpson_paradox` — механизм не воспроизведён

Это не «упрощение», а отсутствие механизма. В этих кейсах доли трафика
в контроле и тесте одинаковые — 46/54 в обеих группах. Парадокс Симпсона
без сдвига микса невозможен по определению: он и есть эффект
перевзвешивания. Агрегатный рост в этих кейсах берётся из расхождения
между итоговой строкой и суммой сегментов, а не из смены весов.

Первый же читатель, знающий, что такое парадокс Симпсона, проверит
и увидит.

### Почему опубликованные цифры пока верны

Агент читает `revenue_effect_relative`, и эталон построен на том же поле.
Вердикты, 42.7%, «20 из 33» и 362/10 внутренне согласованы. Расходятся
сводные колонки с сырыми — то есть пострадает тот, кто станет считать
эффекты сам.

### ВАЖНО: после починки цифры изменятся

Чинить генератор — значит менять данные, а значит и вердикты. Пересчитать
и переписать придётся везде:

- `README.md` — 42.7%, «20 из 33», таблица 362/10/352, структура ошибки
- `docs/index.html` — сетка механизмов (числа зон), полоски распределения,
  карточка «Accuracy 100 → 42.7%», блок «цена слепоты»
- `docs/data/corpus_660.json` и `corpus_stats_660.json` — пересобрать
- `TRAPS.md` — колонка «Агент» по всем 33 механизмам
- **Проверить отдельно:** не опирается ли на старый корпус опубликованная
  статья про SGR (`davydov.my/workspace/articles/sgr-ab-decisions/`)
  и прогоны `v1` в `ab-decisions-bench` — они шли на `cases_auto`,
  но это надо подтвердить, а не предположить

### Завести проверку инвариантов в selfcheck

**Это и есть причина, по которой дефект дожил до публикации.** Сейчас
`selfcheck.py` проверяет только совпадение вердикта агента с эталоном.
Внутренняя согласованность данных не проверяется вообще — корпус может
быть каким угодно внутри, лишь бы агент выдал ожидаемую метку.

Добавить два инварианта:
- сумма сегментов равна строке `all` (там, где сегменты есть);
- `<metric>_effect_relative` равен фактическому изменению среднего
  на пользователя, с допуском.

Прогонять на каждом корпусе, валить при расхождении.

### Оценка: 9–13 часов, отдельной сессией целиком

Разобраться в генераторе 2–3 · починить `gen_simpson_paradox` 1–1.5 ·
согласовать сырые и сводные колонки в общем коде 2–3 · инварианты
в selfcheck 1 · перегенерация и пересборка индексов 0.5 · перепроверка
всех чисел 1–1.5 · разбор изменившихся вердиктов 1–2.

Не разрывать на части: полусобранный корпус хуже старого.

### Проверка, что починено

```
python3 - <<'PY'
import csv, glob, os
bad=0
for cd in sorted(glob.glob("40_ab_factory/vk-style/cases_mvp_v2/case_*")):
    rows=list(csv.DictReader(open(cd+"/data.csv")))
    g=lambda s,v,c: float(next(r[c] for r in rows if r["segment"]==s and r["variant"]==v))
    hit=False
    if {r["segment"] for r in rows}-{"all"}:
        for var in ("control","test"):
            a=g("all",var,"revenue")
            p=sum(float(r["revenue"]) for r in rows if r["segment"]!="all" and r["variant"]==var)
            if abs(a-p)/a*100>0.5: hit=True
    cm=g("all","control","revenue")/g("all","control","n_users")
    tm=g("all","test","revenue")/g("all","test","n_users")
    if abs(g("all","test","revenue_effect_relative")-(tm-cm)/cm)>0.005: hit=True
    bad+=hit
print("битых кейсов:", bad, "— должно быть 0")
PY
```

---

## Проверить остальные механизмы на согласованность другими инвариантами

Две проверки выше покрывают выручку и сегменты. Не проверено:

- `ctr`, `cpm`, `fillrate`, `shows` — согласуются ли между собой
  и с выручкой (например, ожидаемое `revenue ≈ shows × cpm / 1000`);
- `<metric>_p_value` — правдоподобен ли при заявленных размерах выборок
  и разбросе;
- у механизмов без сегментов складывать было нечего, но это не значит,
  что там всё в порядке — просто инвариант неприменим.

Сделать вместе с основной починкой: если найдётся третий класс
расхождений, чинить лучше разом, а не третьей волной.

---

## Предсказание про simpson_paradox — записано ДО починки

Фиксируется заранее, чтобы после починки нельзя было задним числом
объявить любой результат ожидаемым.

**Агент не должен начать ловить `simpson_paradox` после починки.**

Почему: проверка `segment_conflict` в `decision_agent.py` срабатывает
только при разнонаправленных значимых эффектах — нужен хотя бы один
значимый плюс и хотя бы один значимый минус. В правильно собранном
парадоксе Симпсона **оба сегмента отрицательные**, а положителен только
агрегат. Значит конфликт не сработает, агрегат окажется значимым и выше
практического порога, и правило скажет `ship` — как и сейчас.

То есть механизм останется слепой зоной, но уже по правильной причине:
не из-за несходящихся чисел, а потому что порогов для обнаружения
перевзвешивания не существует.

**Если после починки агент начнёт его ловить — искать ошибку, а не
радоваться.** Возможные причины: сегментные p-value случайно вышли
такими, что один сегмент незначим, а другой значим; или сдвиг микса
задан так, что один сегмент вышел в плюс; или изменилась логика
`_extract_signals`.

Записано до внесения правок в генератор.
