# OS
.DS_Store

# Python
__pycache__/
*.pyc

# Generated runs and artifacts
42_workflows/ab_factory/runs/

# Промежуточная генерация — не публикуется.
# cases_auto (100, быстрый пример) и cases_mvp_v2 (660, основной корпус)
# публикуются: без них бенчмарк ab-decisions-bench невоспроизводим,
# он читает корпус отсюда симлинком.
40_ab_factory/vk-style/cases_auto_v2/

# Промежуточные корпуса и черновики итераций — остаются локально.
# Публикуются только cases_auto (100, быстрый пример) и cases_mvp_v2
# (660, основной): они раз-игнорируются отдельным шагом.
40_ab_factory/vk-style/cases_mvp_v1/
40_ab_factory/vk-style/cases_auto_v3/
40_ab_factory/vk-style/cases_probe_*/

# Материалы диагностики подсказок в notes и заглохшей ветки trainer.
# Относятся ко второй публикации (разбор эксперимента), не к корпусу.
# Не мусор: cases_notes_diag — корпус на 100 кейсов по механизмам,
# trainer/trap_zones.json — 33 записи, по одной на механизм.
40_ab_factory/vk-style/cases_notes_diag/
40_ab_factory/vk-style/trainer/
tools/export_trainer_cases.py

# Any data/ directories with generated outputs
**/data/

# But allow docs/data/ (compact JSON for GitHub Pages)
!docs/data/

# secrets — never commit (security.md)
.env

# Реплеи агентов: строятся tools/build_replays_index.py, но ни одна
# страница сайта их не читает — в policy.html остались только строки
# словаря, кода загрузки нет. 812 КБ публиковались впустую. Данные
# восстанавливаются одной командой, поэтому в репозитории не держим.
docs/data/replays/
